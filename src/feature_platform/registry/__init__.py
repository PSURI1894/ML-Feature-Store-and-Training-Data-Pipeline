"""Registry: validate feature definitions and sync them to the metadata store."""

from feature_platform.registry.models import FeatureViewRecord, RegistrySnapshot

__all__ = ["FeatureViewRecord", "RegistrySnapshot"]
